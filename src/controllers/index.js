const notFound = require('./404');
const serverError = require('./500');
const appointmentPage = require('./appointmentsPage');
const getData = require('./getData');
const getPatient = require('./getPatient');
const postPatient = require('./postPatient');

module.exports = {
  notFound,
  serverError,
  getData,
  appointmentPage,
  postPatient,
  postPatient,
};
